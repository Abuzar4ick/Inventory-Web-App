{
  "version": 2,
  "buildCommand": "cd ../frontend && npm install && npm run build",
  "outputDirectory": "../frontend/dist",
  "builds": [
    {
      "src": "src/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "src/index.ts"
    },
    {
      "src": "/(.*)",
      "dest": "../frontend/dist/$1"
    }
  ]
}